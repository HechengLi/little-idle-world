import React from 'react'
import styled from 'styled-components'

const Fist = () => {
  return <div>拳修</div>
}

const StyledFist = styled(Fist)`
  position: absolute;
  left: 0;
  top: 0;
`

export default StyledFist