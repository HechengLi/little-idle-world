import React from 'react'
import styled from 'styled-components'

const Sword = () => {
  return <div>剑修</div>
}

const StyledSword = styled(Sword)`
  position: absolute;
  left: 0;
  top: 0;
`

export default StyledSword