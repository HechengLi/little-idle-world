import React from 'react'
import styled from 'styled-components'

const WorldEntityListItem = ({ entity }) => {
  return (
    <div>{entity}</div>
  )
}

const StyledWorldEntityListItem = styled(WorldEntityListItem)``

export default StyledWorldEntityListItem